/* TODO: Available executables */
const executables = {
  "echo": "Prints to the stdout",
};

/* TODO: Virtual Filesystem */
const filesystem = {
  "/": { }
};

export function interpreteCmd(command) {
  /* TODO: Actually do something >.< */
  return "sh: command not found: " + command;
}
